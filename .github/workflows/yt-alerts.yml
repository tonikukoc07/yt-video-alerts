name: YouTube → Telegram Alerts

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"   # cada 5 minutos (UTC)

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # ♻️ Restaurar state.json para evitar duplicados
      - name: Restore state cache
        uses: actions/cache/restore@v4
        with:
          path: state.json
          key: yt-state-${{ github.run_id }}
          restore-keys: |
            yt-state-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser requests python-telegram-bot==13.15

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          SCAN_LIMIT: "15"
          TZ: "Europe/Madrid"
          PIN_LATEST: "0"
        run: |
          python main.py

      # 💾 Guardar state.json actualizado
      - name: Save state cache
        uses: actions/cache/save@v4
        with:
          path: state.json
          key: yt-state-${{ github.run_id }}
